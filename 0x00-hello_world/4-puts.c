#include<stdio.h>

/**
 * main - Mandatory function which controls the execution of the program.
 *
 * Return: 0 = No errors identified. Non- 0 = Errors detacted.
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
