#include "main.h"

/**
 * _abs - Computes the absolute value of an integer.
 * @n: The integer that wil be checked.
 *
 * Return: The absolute value.
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	return (-n);
}
